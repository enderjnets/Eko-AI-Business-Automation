# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
pgvector==0.2.5

# Cache & Tasks
redis==5.0.4
celery==5.4.0

# AI / LLM
langchain==0.2.0
langchain-openai==0.1.7
langgraph==0.0.59
openai==1.30.0
tiktoken==0.7.0

# Pydantic & Config
pydantic==2.7.1
pydantic-settings==2.2.1

# HTTP Clients
httpx==0.27.0
aiohttp==3.9.5

# Utilities
python-dotenv==1.0.1
email-validator==2.1.1
jinja2==3.1.4
pyyaml==6.0.1
beautifulsoup4==4.12.3

# Web Scraping (Phase 1: AsyncFetcher for anti-bot + TLS impersonation).
# Scrapling pulls a deep dep chain (browserforge, camoufox, playwright, ...);
# we pin them here so fresh docker builds don't hit ImportError silently.
scrapling==0.4.8
curl_cffi==0.7.4
tldextract==5.1.2
playwright==1.45.0
browserforge==1.2.4
camoufox==0.4.11
cssselect==1.2.0
orjson==3.10.7

# Security
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0

# Email
resend==2.1.0

# Local embeddings (alternative to OpenAI)
sentence-transformers==3.0.1

# Monitoring
sentry-sdk==2.1.0
stripe>=8.0.0
langdetect==1.0.9
